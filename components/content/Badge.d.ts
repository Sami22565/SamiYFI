export interface BadgeProps {
  children: React.ReactNode;
  tone?: 'ember' | 'gold' | 'dark';
}

/**
 * @startingPoint section="Components" subtitle="Eyebrow / status pill" viewport="700x120"
 */
export function Badge(props: BadgeProps): JSX.Element;
