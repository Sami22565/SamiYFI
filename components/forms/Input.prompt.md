Labeled text field for forms — application intake, contact, login.

```jsx
<Input label="Full name" placeholder="Jane Doe" name="fullName" value={v} onChange={onChange} />
```

Supports `type` = text/email/tel/number/password. Label is optional (uppercase caption style).
